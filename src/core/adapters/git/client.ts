/**
 * Temporary re-export shim — the implementation relocated to
 * `adapters/vcs/git/git-cli/client.ts` (vcs adapter seam, w2dlu). Deleted in T7
 * once every consumer imports the VcsGitAdapter surface instead.
 */

export * from "../vcs/git/git-cli/client.js";
